const start_message = '';
const help_message = '';
const answer_message = '';

module.exports = function () {
  return {
    start: start_message,
    help: help_message,
    answer: answer_message
  };
}();